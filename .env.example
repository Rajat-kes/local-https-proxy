# Copy to .env and adjust (`.env` is gitignored).
# Shell exports override these values.

PORT=8085
BIND_HOST=localhost
DEFAULT_TARGET=http://10.212.43.57:9220

# Optional TLS paths (defaults: ./certificate/)
# CERT_DIR=
# CERT_KEY_PATH=
# CERT_CRT_PATH=
